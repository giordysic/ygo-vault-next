export default function AnalyticsPage() {
  return (
    <div>
      <h1 style={{ color: 'var(--text-primary)', fontSize: 'var(--text-2xl)', fontWeight: 'var(--font-weight-bold)' }}>
        Analytics
      </h1>
      <p style={{ color: 'var(--text-secondary)', marginTop: 'var(--space-2)' }}>
        Collection analytics will appear here.
      </p>
    </div>
  );
}
